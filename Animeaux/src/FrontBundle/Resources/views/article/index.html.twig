{% extends 'base.html.twig' %}

{% block body %}




{#

    <h1>Articles list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nomarticle</th>
                <th>Iduser</th>
                <th>Nbvue</th>
                <th>Description</th>
                <th>Nomimage</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td><a href="{{ path('article_show', { 'id': article.id }) }}">{{ article.id }}</a></td>
                <td>{{ article.nomArticle }}</td>
                <td>{{ article.idUser }}</td>
                <td>{{ article.nbVue }}</td>
                <td>{{ article.description }}</td>
                <td>{{ article.nomImage }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('article_show', { 'id': article.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('article_edit', { 'id': article.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('article_new') }}">Create a new article</a>
        </li>
    </ul>

    #}




    <div id="blog-home" class="page">
        <div class="container">
            <div class="row">
                <!-- Blog Entries Column -->
                <div class="col-lg-9 page-with-sidebar">
                    <!-- Blog Post -->
                    <h2>Articles</h2>
                    <span class="h7 mt-0"></span>
                    <hr class="small-divider left">

                    {% if is_granted('ROLE_VETERINAIRE') -%}

             <a href="{{ path('article_new') }}" class="btn btn-info" >Create a new article</a>


                    {%- endif %}






                    {% for article in articles %}


                    <div class="card blog-card pattern1">
                        <a href="{{ path('article_show', { 'id': article.id }) }}">
                            <!-- image -->
                            <div class="blog-img">
                                <img class="card-img-top img-fluid" src="{{ asset('images/') }}{{ article.nomImage }}" alt="">
                            </div>
                            <!-- /blog-img -->
                        </a>
                        <div class="card-body">
                            <a href="{{ path('article_show', { 'id': article.id }) }}">
                                <h3 class="card-title">{{ article.nomArticle }}</h3>
                            </a>
                            <div class="post-info text-muted">
                                {{ article.date_creation|date }} by
                                {{ article.idUser }} / {# <a href="#"><i class="fas fa-eye margin-icon"></i>{{ nbrVue|length}}: Vues</a>   #}
                            </div>
                            <!-- excerpt -->
                            <a href="{{ path('article_show', { 'id': article.id }) }}" class="btn btn-secondary">Read More →</a>
                        </div>
                        <!--card-footer -->
                    </div>
                    <!-- /card blog-card -->


                    {% endfor %}






                    <!-- /col-md -->
                </div>
                <!-- /page-with-sdiebar -->
                <!-- Sidebar -->
                <!--/sidebar -->      </div>
            <!-- /.row -->
        </div>
        <!-- /.container -->



    </div>

{% endblock %}
